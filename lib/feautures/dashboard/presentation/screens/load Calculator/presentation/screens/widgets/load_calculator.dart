import 'package:flutter/material.dart';
class LoadCalculator extends StatelessWidget {
  const LoadCalculator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
